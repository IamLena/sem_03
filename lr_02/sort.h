#ifndef SORT_H
#define SORT_H
#include "derectives.h"

k_el *sort_keys(ft *flats, int length);
ft *sort_flats(ft *flats, int length);
k_el *sort_keys_shaker(ft *flats, int length);
ft *sort_flats_shaker(ft *flats, int length);

#endif // SORT_H
